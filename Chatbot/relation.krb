# Forward chaining based relations between facts
# verify pizza
# pizzacontains(pep, topping)

ask_for_food

ask_for_toppings

ask_for_pizza_veg
    foreach
        fact.type($type)
        fact.size($size)
        fact.crust($crust)
        fact.sauce($sauce, $vegetarian)
        check $vegetarian != non-veg
        fact.cheese($cheese)
        fact.topping((($name, $veg), )
    assert
        fact.pizza($type, $size, $crust, $sauce, (*$_), $vegetarian)

